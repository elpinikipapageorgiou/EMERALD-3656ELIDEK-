# Inference of FCM
The weight matrix that is attained from the FCM training process is available for inference.

# Usage
The user can fill the dataset Excel file, and for every instance the FCM will predict it.

# Normalization of columns
columns_to_normalize = ['AGE', 'BMI']

# Results
For every instance, the prediction will be printed, along with the Plot with demonstrating the input-output interconn

# Data

dataset.xlsx :  Enter the CAD instances